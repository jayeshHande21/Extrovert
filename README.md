# Extroverts Signup Wizard

Frontend-only replica of the Extroverts app landing, terms, and 4-step signup.

## Scripts

```bash
npm install
npm run dev
npm run build
```

Routes: `/` landing, `/terms`, `/signup`, `/welcome`.
